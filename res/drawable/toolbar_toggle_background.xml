<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<!--
     Background of the toolbar toggles: a ripple over a 40dp disc that fades in while the
     button is checked. The display they sit on is colorSurface, so the disc takes the highest
     surface container tone to stand out from it.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <inset android:inset="@dimen/toolbar_toggle_inset">
            <shape android:shape="oval">
                <solid android:color="@android:color/white" />
            </shape>
        </inset>
    </item>
    <item>
        <inset android:inset="@dimen/toolbar_toggle_inset">
            <selector
                android:enterFadeDuration="@integer/toolbar_toggle_duration"
                android:exitFadeDuration="@integer/toolbar_toggle_duration">
                <item android:state_checked="true">
                    <shape android:shape="oval">
                        <solid android:color="?attr/colorSurfaceContainerHighest" />
                    </shape>
                </item>
                <item>
                    <shape android:shape="oval">
                        <solid android:color="@android:color/transparent" />
                    </shape>
                </item>
            </selector>
        </inset>
    </item>
</ripple>
